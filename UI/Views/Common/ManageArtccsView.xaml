<adonisControls:AdonisWindow 
    x:Class="vFalcon.UI.Views.Common.ManageArtccsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:vFalcon"
    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
    xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
    xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
    mc:Ignorable="d"
    Title="Manage ARTCCs"
    MinWidth="400" Width="400" Height="500"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    FontFamily="Consolas" FontSize="14"
    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
        <ScrollViewer Margin="10">
            <ItemsControl ItemsSource="{Binding InstalledArtccs}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock 
                                    Grid.Column="0"
                                    Text="{Binding Name}"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Foreground="{Binding ArtccStatusTextForeground, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock
                                    FontSize="10"
                                    Grid.Column="0" Grid.Row="1"
                                    Text="{Binding StatusText}"
                                    VerticalAlignment="Center"
                                    Foreground="{Binding StatusTextForeground, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button 
                                    Grid.Column="1" Grid.RowSpan="2"
                                    Content="{Binding InstallUninstallText, UpdateSourceTrigger=PropertyChanged}" 
                                    Width="100" Height="24"
                                    Command="{Binding DataContext.InstallUninstallCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}">
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Border>
</adonisControls:AdonisWindow>
