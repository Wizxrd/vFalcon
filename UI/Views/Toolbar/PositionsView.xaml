<adonisControls:AdonisWindow 
    x:Class="vFalcon.UI.Views.Toolbar.PositionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
    xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
    xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
    xmlns:local="clr-namespace:vFalcon.UI.Views.Toolbar"
    IconVisibility="Collapsed"
    ResizeMode="NoResize"
    Title="Positions"
    Height="400" MinWidth="300" Width="300"
    FontFamily="Consolas" FontSize="14"
    SizeToContent="Width"
    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock
                Grid.Row="0"
                Margin="10,5,10,0"
                FontWeight="Bold"
                Text="Facility"/>
            <ComboBox
                Grid.Row="1"
                Margin="10,5,10,0"
                ItemsSource="{Binding Facilities, UpdateSourceTrigger=PropertyChanged}"
                SelectedValue="{Binding SelectedFacility, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <ScrollViewer Margin="5" Grid.Row="2">
                <ItemsControl ItemsSource="{Binding Positions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox
                            Height="24"
                            Content="{Binding Name}"
                            IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Command="{Binding Command}"
                            CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</adonisControls:AdonisWindow>
