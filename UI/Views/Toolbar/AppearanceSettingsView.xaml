<adonisControls:AdonisWindow 
    x:Class="vFalcon.UI.Views.Toolbar.AppearanceSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:vFalcon.UI.Views.Toolbar"
    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
    xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
    xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
    IconVisibility="Collapsed"
    Title="Appearance Settings"
    FontFamily="Consolas" FontSize="12"
    ResizeMode="NoResize"
    SizeToContent="Height"
    Width="250" Height="Auto"
    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
        <StackPanel>
            <Label Content="Background" Margin="10,5,0,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,0" Minimum="0" Maximum="60" TickFrequency="2" IsSnapToTickEnabled="True" Value="{Binding Background}"/>
            <Label Content="Backlight" Margin="10,5,0,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,0" Minimum="0" Maximum="100" TickFrequency="2" IsSnapToTickEnabled="True" Value="{Binding Backlight}"/>
            <Label Content="Datablock Font Size" Margin="10,5,0,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,0" Minimum="8" Maximum="24" TickFrequency="2" IsSnapToTickEnabled="True" Value="{Binding DatablockFontSize}"/>
            <Label Content="Full Datablock Brightness" Margin="10,5,0,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,0" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding FullDatablockBrightness}"/>
            <Label Content="Limited Datablock Brightness" Margin="15,5,5,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,0" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding LimitedDatablockBrightness}"/>
            <Label Content="Map Brightness" Margin="10,5,0,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,0" Minimum="0" Maximum="100" TickFrequency="2" IsSnapToTickEnabled="True" Value="{Binding MapBrightness}"/>
            <Label Content="History Brightness" Margin="10,5,0,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,0" Minimum="0" Maximum="100" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding HistoryBrightness}"/>
            <Label Content="History Length" Margin="10,5,0,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,0" Minimum="0" Maximum="8" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding HistoryLength}"/>
            <Label Content="Velocity Vector" Margin="10,5,0,0" FontWeight="Regular"/>
            <Slider Margin="10,5,10,10" Minimum="0" Maximum="5" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding VelocityVector}"/>
        </StackPanel>
    </Border>
</adonisControls:AdonisWindow>
