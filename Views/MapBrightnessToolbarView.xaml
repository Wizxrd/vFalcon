<UserControl x:Class="vFalcon.Views.MapBrightnessToolbarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:vFalcon.Views"
             xmlns:controls="clr-namespace:vFalcon.Resources.Controls"
             xmlns:converters="clr-namespace:vFalcon.Converters"
             mc:Ignorable="d" 
             Width="Auto" Height="75">
    <UserControl.Resources>
        <converters:BoolToCollapsedConverter x:Key="BoolToCollapsedConverter"/>
    </UserControl.Resources>
    <Border Padding="0" BorderBrush="{StaticResource MasterToolbarBackground}" BorderThickness="2">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <controls:EramButton
                Grid.Column="0" Grid.Row="0"
                VerticalAlignment="Top"
                Line1Text="MAP"
                Line2Text="BRIGHT"
                EramButtonStyle="{StaticResource MenuButtonOpen}"
                Command="{Binding MapBrightnessBackCommand}"/>
            <Border Grid.Column="1" Background="{StaticResource MasterToolbarBackground}" BorderBrush="#896800" BorderThickness="2" Margin="0,-2,0,2">
                <Grid>
                    <ItemsControl 
                        x:Name="DefaultLayout" 
                        ItemsSource="{Binding BrightnessBcg}"
                        Visibility="{Binding UseAlternateMapLayout, Converter={StaticResource BoolToCollapsedConverter}, ConverterParameter=True}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="3"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="3"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                </Grid>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Grid.Row" Value="{Binding Row}"/>
                                <Setter Property="Grid.Column" Value="{Binding Column}"/>
                            </Style>
                        </ItemsControl.ItemContainerStyle>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <controls:NoTearoffButton
                                    NoTearoffLine1Text="{Binding LabelLine1}"
                                    NoTearoffLine2Text="{Binding LabelLine2}"
                                    NoTearoffStyle="{StaticResource IncDecButton}"
                                    PreviewMouseDown="MapBrightButtonPreviewMouseDown">
                                </controls:NoTearoffButton>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
